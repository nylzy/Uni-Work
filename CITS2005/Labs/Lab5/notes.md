# notes

- there is a difference between declaring and initialising
    - declaring: stating that a variable exists and what type it is
    - intialising it: e.g. allocating space for an array
- even loop variables need to be declared (e.g. 'int i = 0')
